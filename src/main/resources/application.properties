
#quarkus.native.additional-build-args=-H:+DashboardJson

# Index jjq-jakarta for JAX-RS provider auto-discovery (JqValueMessageBodyReader/Writer)
quarkus.index-dependency.jjq-jakarta.group-id=io.hyperfoil.tools
quarkus.index-dependency.jjq-jakarta.artifact-id=jjq-jakarta

# registering dummy datasource for each supported backend so that the drivers are not removed by dead code elimination
quarkus.datasource."dummy-sqlite".db-kind=sqlite
quarkus.datasource."dummy-sqlite".active=false
quarkus.datasource."dummy-postgresql".db-kind=postgresql
quarkus.datasource."dummy-postgresql".active=false

# H5mDriver delegates to sqlite or postgresql based on URL
quarkus.datasource.db-kind=postgresql
quarkus.datasource.jdbc.driver=io.hyperfoil.tools.h5m.provided.H5mDriver

quarkus.datasource.username=${DB_USERNAME:quarkus}
quarkus.datasource.password=${DB_PASSWORD:quarkus}
quarkus.datasource.jdbc.acquisition-timeout=PT30S
quarkus.datasource.jdbc.initial-size=1
quarkus.datasource.jdbc.min-size=1
quarkus.datasource.jdbc.max-size=10

#quarkus.hibernate-orm.log.sql=true
quarkus.hibernate-orm.dialect=io.hyperfoil.tools.h5m.provided.H5mDialect
quarkus.hibernate-orm.schema-management.strategy=update
quarkus.hibernate-orm.fetch.batch-size=100
quarkus.hibernate-orm.jdbc.statement-batch-size=100
quarkus.hibernate-orm.order-inserts=true
quarkus.hibernate-orm.order-updates=true

# Second-level cache (Hibernate @Immutable + READ_ONLY entities)
quarkus.hibernate-orm.second-level-caching-enabled=true
quarkus.hibernate-orm.cache."io.hyperfoil.tools.h5m.entity.ValueEntity".memory.object-count=10000
quarkus.hibernate-orm.cache."io.hyperfoil.tools.h5m.entity.NodeEntity".memory.object-count=500
quarkus.hibernate-orm.physical-naming-strategy=org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy

quarkus.banner.enabled=false

# CLI mode: disable HTTP server and default to SQLite (works out of the box)
%cli.quarkus.aesh.start-console=true
%cli.quarkus.http.host-enabled=false
%cli.quarkus.management.enabled=true
%cli.quarkus.management.port=8080
# disable message about quinoa being disabled
%cli.quarkus.log.category."io.quarkiverse.quinoa.deployment.QuinoaProcessor".level=ERROR
#default cli to sqlite
# Override with -Dquarkus.datasource.jdbc.url=jdbc:postgresql://host:port/db
# H5mDialect auto-detects the right dialect (SQLite or PostgreSQL) from JDBC URL at startup
%cli.quarkus.datasource.jdbc.url=jdbc:sqlite:${H5M_PATH:${user.home}/h5m.db}
%cli.quarkus.datasource.devservices.enabled=false
%cli.quarkus.quinoa=false
# disable OIDC
%cli.quarkus.oidc.tenant-enabled=false
%cli.quarkus.oidc-proxy.enabled=false
# CLI uses uber-jar for single-file distribution. Truffle Multi-Release check is disabled since uber-jar merging loses the Multi-Release manifest attribute.
%cli.quarkus.package.jar.type=uber-jar
%cli.quarkus.package.jar.add-runner-suffix=false

# CORS for frontend development (dev profile only)
%dev.quarkus.http.cors=true
%dev.quarkus.http.cors.origins=/.*/
%dev.quarkus.http.cors.methods=GET,POST,PUT,DELETE,OPTIONS
%dev.quarkus.http.cors.headers=content-type,authorization

quarkus.http.limits.max-body-size=200M

quarkus.package.jar.type=fast-jar

# open api
quarkus.smallrye-openapi.operation-id-strategy=method
quarkus.smallrye-openapi.store-schema-directory=src/main/webui/

# REST endpoints mounted under /api so the SPA fallback owns the root paths
quarkus.rest.path=/api

# quinoa
quarkus.quinoa.enable-spa-routing=true
quarkus.quinoa.dev-server.direct-forwarding=true

# aesh configuration: runtime for commands, console for REPL
quarkus.aesh.start-console=false
quarkus.aesh.mode=console
quarkus.aesh.prompt=[h5m]\$ ${empty:}
quarkus.aesh.persist-history=true
quarkus.aesh.history-file=.h5m_history
quarkus.aesh.history-size=1000
quarkus.aesh.enable-alias=true

#this disables the INFO messages from the cli
quarkus.log.category."io.quarkus".level=ERROR
quarkus.log.category."org.hibernate".level=ERROR

quarkus.log.category."org.hibernate.SQL_SLOW".level=INFO
quarkus.hibernate-orm.log.queries-slower-than-ms=100

#for debugging production jar
#10m timeout
quarkus.transaction-manager.default-transaction-timeout=PT10m

# ApiKey Security
h5m.api-key.expiration-days=365

# Disable proactive auth so unauthenticated requests can reach @PermitAll endpoints
quarkus.http.auth.proactive=false

# OIDC
quarkus.oidc.tenant-enabled=true
quarkus.oidc.client-id=h5m
quarkus.oidc.application-type=hybrid
quarkus.oidc-proxy.external-client-id=h5m-webui
quarkus.oidc-proxy.external-client-secret=public

# Dev mode: enable lightweight OIDC server
%dev.quarkus.oidc.devservices.enabled=true

# Mailer - configure via environment for service deployments
quarkus.mailer.from=${MAILER_FROM:h5m@localhost}
quarkus.mailer.host=${MAILER_HOST:localhost}
quarkus.mailer.port=${MAILER_PORT:25}
quarkus.mailer.mock=true

%test.h5m.slack.api.url=http://localhost:19876/api/chat.postMessage
h5m.slack.api.url=https://slack.com/api/chat.postMessage

# Container image (JIB) — built via mvn package -Dquarkus.container-image.build=true
quarkus.container-image.group=hyperfoil
quarkus.container-image.name=h5m
quarkus.container-image.registry=ghcr.io
# Default to not building images during normal mvn package
quarkus.container-image.build=false
